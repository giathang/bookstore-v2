<% if @book.category.nil? %>
  <% breadcrumb :no_category_book, @book %>
<% else %>
  <% breadcrumb :book, @book %>
<% end %>

<div class="page-header">
  <h1>Editing Book</h1>
</div>


<%= render 'form' %>

<%= link_to 'Show', @book, class: 'btn btn-default' %> |
<%= link_to 'Back', books_path, class: 'btn btn-default' %>
